/*
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define EPS 1e-9
#define fixed(n) cout << fixed << setprecision(n)
#define all(v) ((v).begin()), ((v).end())
#define IOS ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
int main() {
    IOS;
   ll n; cin>>n;
   ll arr[n];
   for(int i=0;i<n;i++) cin>>arr[i];

   int y,res=0; cin>>y;
   for(int i=0;i<n;i++){
        if(arr[i]==y) {
        cout<<i<<endl;
        res=1; // awl raqm yla2eh == y ,msh 3ayzenoh ykml lf
        break;
    }
}

if(res==0) cout<<-1;
}
*/

