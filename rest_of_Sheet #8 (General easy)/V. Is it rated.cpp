/*
#include <bits/stdc++.h>
#include<unordered_map>
using namespace std;
typedef long long ll;
#define fixed(n) cout << fixed << setprecision(n)
#define IAMTRYINGandCRYING ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define sz(x) int(x.size())
int arr[100];
int main() {
    IAMTRYINGandCRYING;
    int n,x,y,mn,flag1=0,flag2=0; cin>>n;
    while(n--){
        cin>>x>>y;
        if(x!=y) flag1=1;
        else mn=min(mn,x);
        if(mn<x)flag2=1;

    }
    if(flag1) cout<<"rated";
    else if(flag2) cout<<"unrated";
    else cout<<"maybe";
}*/
