/*
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define all(v) ((v).begin()), ((v).end())
#define IOS ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
int main() {
    IOS;
    int num_of,n,even=0,odd=0,nega=0,posi=0; cin>>num_of;
    for(int i=0;i<num_of;i++){
        cin>>n;
        if(n%2==0) even++;
        else odd++;

        if(n<0) nega++;
        else if(n>0) posi++;
    }
    cout<<"Even: "<<even<<endl<<"Odd: "<<odd<<endl<<"Positive: "<<posi<<endl<<"Negative: "<<nega<<endl;


    return 0;
}
*/
