/*
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define all(v) ((v).begin()), ((v).end())
#define IOS ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
int main() {
    IOS;
    int x,y,t,sum=0; cin>>t;
    while(t--){
        cin>>x>>y;
        if(x>y) swap(x,y);
        for(int i=x+1;i<y;i++){
            if(i%2!=0) sum+=i;
        }
        cout<<sum<<endl;
        sum=0;
    }

}*/
