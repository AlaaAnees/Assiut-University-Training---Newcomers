/*
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define all(v) ((v).begin()), ((v).end())
#define IOS ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
int main() {
    IOS;
int n,m,sum=0;

while(true) {
    cin >> n >> m;
    if (n > m) swap(n, m);
    if (n <= 0 || m <= 0) return 0;
    for (int i = n; i <= m; i++) {
        sum += i;
        cout << i << " ";
    }
    cout<<"sum ="<<sum<<endl;
    sum=0;
}
    return 0;
}*/
